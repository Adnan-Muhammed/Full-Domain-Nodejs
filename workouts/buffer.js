const buffer = Buffer.from('apple');

// Log the Buffer
console.log('Buffer value:', buffer);

// Log the Buffer as a hexadecimal string
console.log('Hexadecimal representation:', buffer.toString('hex'));
